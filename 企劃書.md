````markdown
# 自動化交易策略機器人專案企劃書

> 版本：v0.1  
> 撰寫人：Chun（暫）  
> 目標：打造一套「可回測、可維護、可持續優化」的自動化交易策略機器人系統，初期以加密貨幣（例如 BTC/ETH）為主，採用 SMC / ICT 風格策略（NY Session + Liquidity Sweep + FVG + RR 1:3）。

---

## 目錄

1. [使用情境（Use Cases）](#使用情境use-cases)  
2. [硬體設置（Infrastructure / Hardware）](#硬體設置infrastructure--hardware)  
3. [軟體設計（Software Design Overview）](#軟體設計software-design-overview)  
4. [專案架構（Project Structure）](#專案架構project-structure)  
5. [開發流程（Development Workflow）](#開發流程development-workflow)  
6. [使用技術（Tech Stack）](#使用技術tech-stack)  
7. [使用的策略（Trading Strategy — ICT/SMC 模型）](#使用的策略trading-strategy--ictsmc-模型)  
8. [風險控管與安全設計](#風險控管與安全設計)  
9. [監控、告警與維運](#監控告警與維運)  
10. [未來擴充方向](#未來擴充方向)

---

## 使用情境（Use Cases）

### 1. 個人量化交易研究與實盤部署

- 使用者平時在本機進行策略研究、回測與調參。
- 選定具有 edge 的策略後，將程式部署到 VPS 或雲端伺服器 24/7 運行。
- 不需依賴家中桌電是否開機或是否連線。

### 2. 多策略組合與分帳戶管理

- 將不同策略（例：  
  - ICT NY Session FVG 策略  
  - 趨勢追蹤策略  
  - 區間套利策略）  
  分別綁定不同子帳戶或不同 API Key。
- 系統可同時運行多個 bot，彼此風險隔離。

### 3. 策略快速迭代與回歸測試

- 每當有新的策略想法：  
  1. 先在 Notebook 進行 prototype。  
  2. 再轉換成標準化 Strategy 類別。  
  3. 用統一的回測框架跑歷史測試。
- 確保「所有策略都在同一個評估標準下比較」。

### 4. 遠端監控與通知

- 當系統產生新訂單、平倉、遇到異常時：
  - 推送訊息到 Telegram / Email / Discord。
- 使用者即使不在家、只拿手機，也能掌握當前部位與資金情況。

---

## 硬體設置（Infrastructure / Hardware）

### 1. 本地開發環境（開發機）

- 用途：  
  - 撰寫程式、回測、策略研究、版本控管。
- 建議：
  - PC / 筆電（Windows + WSL2 或 macOS 均可）
  - 建議規格：
    - CPU：4 核以上
    - RAM：16 GB 以上（方便跑多次回測）
    - SSD：> 512 GB（儲存歷史 K 線資料）

### 2. 雲端伺服器 / VPS（正式執行環境）

**核心理念：自動交易不要依賴家裡桌電，要用 VPS。**

- 用途：
  - 24/7 持續運轉自動交易程式
  - 即時抓行情、判斷、下單
- 建議規格（單個 VPS）：
  - CPU：1–2 vCPU
  - RAM：1–2 GB
  - Disk：20–40 GB SSD
- 作業系統：
  - Ubuntu Server LTS（例如 22.04）
- 選擇條件：
  - 穩定性：良好 uptime / SLA
  - 網路品質：連接主要交易所延遲低
  - 固定 IP：便於設定 API 白名單

### 3. 網路與電力考量

- 本地開發機：偶爾關機無妨，對實際交易無影響。
- VPS：由供應商負責供電與網路穩定。
- 風險緩解：
  - 若 VPS 供應商出狀況，可在另一家備妥 docker image，一鍵搬移。

---

## 軟體設計（Software Design Overview）

目標：  
> 讓「策略邏輯」與「交易所實作」與「回測引擎」**彼此解耦**，方便維護與擴充。

### 核心模組概念

1. **Strategy 模組（策略邏輯）**
   - 負責判斷：  
     - 是否進場？做多 / 做空？  
     - SL / TP 在哪裡？  
   - 不關心：
     - 交易所 API 細節
     - 下單實作
     - 資金配置

2. **Exchange 模組（交易所抽象層）**
   - 統一封裝：
     - `fetch_ohlcv()`：抓 K 線 / 價格資料
     - `create_order()`：建立掛單 / 市價單
     - `fetch_balance()`：查詢餘額
   - 可有多個實作：
     - `BinanceExchange`
     - `OKXExchange`
     - `MockExchange`（回測 / 模擬用）

3. **Backtest 模組（回測引擎）**
   - 對 Strategy／Exchange 提供一個模擬歷史走勢的環境：
     - 逐根 K 線推進
     - 模擬下單、成交、打到 SL / TP
     - 計算 PnL、最大回撤、勝率等指標

4. **Live Trading 模組（實盤執行）**
   - Event Loop：
     - 定時從交易所抓最新 K 線
     - 丟給 Strategy 判斷
     - 透過 Exchange 實作實際下單
   - 負責：
     - 風險控管（倉位大小、單日最大虧損）
     - 重試機制（API 失敗重試）
     - 記錄 log，發送通知

5. **Config / Risk / Utils**
   - Config：集中管理所有設定（商品、時間、參數）
   - Risk：風險管理與倉位計算
   - Utils：時間處理、時區轉換、logging 設定等

---

## 專案架構（Project Structure）

以下為建議的資料夾架構，目標是「清楚分層、易於擴充」：

```text
trading-bot/
├─ README.md                 # 專案說明
├─ requirements.txt          # Python 套件清單
├─ .env.example              # API Key 等環境變數樣板（不含真實金鑰）
├─ config/
│  ├─ config.yaml            # 全域設定（商品、策略參數、風險管理）
│  └─ logging.yaml           # 日誌設定格式
├─ data/
│  ├─ raw/                   # 原始歷史 K 線
│  └─ backtest/              # 回測用整理後資料
├─ notebooks/
│  ├─ 01_explore_ict_ny_fvg.ipynb   # ICT 策略探索
│  └─ ...
├─ core/
│  ├─ __init__.py
│  ├─ strategy_base.py       # 抽象 Strategy 介面
│  ├─ strategy_ict_ny_fvg.py # ICT NY FVG 策略
│  ├─ exchange_base.py       # 抽象 Exchange 介面
│  ├─ exchange_mock.py       # 模擬交易所（回測 / Paper Trading）
│  ├─ exchange_binance.py    # Binance 實作（可擴充 OKX 等）
│  ├─ risk.py                # 倉位計算、最大虧損、槓桿限制
│  ├─ utils_time.py          # 時區、Session 計算（NY / London 等）
│  └─ utils_logging.py       # 日誌初始化
├─ backtest/
│  ├─ __init__.py
│  └─ backtest_runner.py     # 通用回測入口點
├─ live/
│  ├─ __init__.py
│  ├─ bot_runner.py          # 實盤交易主程式
│  └─ paper_runner.py        # 模擬 / Demo 帳戶用
└─ scripts/
   ├─ download_history.py    # 歷史資料下載工具
   └─ deploy_to_vps.sh       # 簡易部署腳本（git pull + docker restart）
````

---

## 開發流程（Development Workflow）

### Phase 0：策略構思與 TradingView 驗證

1. 在 TradingView 上用 Pine Script 快速畫出策略條件：

   * NY Session 區塊
   * Liquidity Sweep 標註（掃高/掃低）
   * FVG 區域（Fair Value Gap）
   * 進/出場點（RR 1:3）
2. 用肉眼觀察：

   * 行為是否符合預期？
   * 是否常出現「一看就很雷」的訊號？

---

### Phase 1：Notebook 原型（Prototype）

在 `notebooks/01_explore_ict_ny_fvg.ipynb`：

1. 使用交易所 API / 檔案載入歷史 K 線到 pandas DataFrame。
2. 在 Notebook 內實作：

   * session 過濾（NY 開盤後 2 小時）
   * sweep 判斷
   * FVG 偵測
   * 進場條件和 SL / TP
3. 粗略計算：

   * trade 數量
   * 勝率
   * 平均 RR
   * 簡單 equity curve

目標：**證明這個策略有基本 edge，才繼續投資開發時間。**

---

### Phase 2：抽象成 Strategy 類別

在 `core/strategy_ict_ny_fvg.py`：

* 定義一個繼承 `StrategyBase` 的類別，例如：

```python
class ICTNYFVGStrategy(StrategyBase):
    def on_bar(self, bar, history) -> Signal:
        """
        bar: 當前 K 線
        history: 過去數根 K 線資料
        回傳：Signal (e.g., LONG / SHORT / FLAT / CLOSE)
        """

    def get_sl_tp(self, entry_price, direction, bar, history):
        """
        根據策略邏輯計算 SL 與 TP。
        """
```

此時的目標是：

> **Strategy 完全不知道「交易所」是誰、也不知道「資金有多少」，只負責判斷「做不做」。**

---

### Phase 3：回測引擎（Backtest）

在 `backtest/backtest_runner.py`：

1. 載入：

   * 歷史資料（DataFrame）
   * 指定策略（`ICTNYFVGStrategy`）
2. 撰寫迴圈：

   * 逐根時間排序好的 K 線：

     * 把當前 `bar` 與 `history` 傳入 `strategy.on_bar()`
     * 若策略要求開倉：

       * 依照 `risk.py` 計算倉位大小
       * 根據 `get_sl_tp()` 設定 SL / TP
     * 模擬後續 K 線是否先打到 SL 或 TP
3. 輸出結果：

   * trade list（CSV）
   * summary 指標：

     * 勝率、平均盈虧、淨利
     * 最大回撤（MDD）
     * Sharpe / Calmar（未來可再補）

---

### Phase 4：接上 MockExchange 進行 Paper Trading

在 `live/paper_runner.py`：

1. 使用 `exchange_mock.py` 建立一個模擬帳戶：

   * 初始資金例如 10,000 USDT
   * 所有下單都在記憶體中模擬
2. 實作一個簡單的 event loop：

   * 每 X 秒「假裝」收到一根新的 K 線（可用歷史資料或 replay）
   * 丟給策略判斷
   * 透過 MockExchange 模擬交易
3. 檢查：

   * 程式是否能長時間運作？
   * 是否會因錯誤中斷？
   * log 是否清楚可讀？

---

### Phase 5：交易所實作（Binance / OKX）

在 `core/exchange_binance.py`：

* 透過 `ccxt` 或官方 SDK 實作：

  * 讀取最新 K 線／Ticker
  * 建立真實掛單 / 市價單
  * 查詢餘額與持倉
* 將 API Key 放在 `.env`（不進 Git）：

  * `BINANCE_API_KEY=...`
  * `BINANCE_API_SECRET=...`

在 `live/bot_runner.py`：

1. 建立 `BinanceExchange` 實例。
2. 讀取 `config.yaml` 決定：

   * 交易商品（symbol）
   * 每筆風險 %（例：1% of equity）
3. 啟動 loop：

   * 定時拉最新 K 線
   * 丟給策略計算訊號
   * 呼叫 Exchange 下單
4. 串接通知系統（Telegram 等）回報下單、平倉與錯誤。

---

### Phase 6：部署到 VPS

1. 在 VPS 安裝：

   * `git`, `python3`, `pip`, `tmux` 或 `docker`
2. clone 專案：

   ```bash
   git clone <repo_url>
   cd trading-bot
   pip install -r requirements.txt
   ```
3. 使用 `tmux` 運行：

   ```bash
   tmux new -s bot
   python live/bot_runner.py
   # 離開：Ctrl+B 然後 D
   ```
4. 之後更新程式：

   ```bash
   cd trading-bot
   git pull
   tmux attach -t bot  # 重啟程式
   ```

---

## 使用技術（Tech Stack）

### 語言與主要工具

* **Python 3.11+**

  * pandas：資料處理
  * numpy：數學運算
  * ccxt：多交易所 API 封裝
  * loguru 或 logging：日誌紀錄
  * python-dotenv：環境變數管理
  * jupyter：研究與原型開發

* **TradingView / Pine Script**

  * 用於視覺化策略、找出可能 edge、做快速驗證

* **Git / GitHub / GitLab**

  * 版本控制
  * 不將 API Key、真實 log 推上遠端

### 系統與部署

* OS：Ubuntu（VPS）、Windows / macOS（開發）
* 遠端管理：

  * SSH（Termius、VSCode Remote SSH）
* 容器化（未來可加）：

  * Docker + docker-compose

### 通知與監控

* Telegram Bot / Discord Webhook

  * 通知：

    * 進場、出場
    * 重大錯誤
    * VPS 重啟後 bot 重啟成功

---

## 使用的策略（Trading Strategy — ICT/SMC 模型）

### 策略名稱

> ICT NY FVG Liquidity Strategy

### 基本概念

* 架構源於 **SMC / ICT** 思維：

  * Liquidity（流動性池）
  * Sweep / Stop-hunt（掃高掃低）
  * FVG（Fair Value Gap，不平衡區）
  * OB（Order Block）
  * NY Session Killzone（紐約開盤時間）

### 時間框架與商品

* 時間框架：5 分鐘 K 線
* 商品：BTCUSDT（可延伸至 ETH 或其他流動性良好標的）

### 多頭條件（Long Setup）

1. **Session 限制**

   * 僅在 NY Session（例：交易所時間 09:30–11:30）內搜尋訊號。

2. **Liquidity Sweep（掃低）**

   * 最近幾根 K 線中，出現「當前 low < 前一根 low」的情況。
   * 判定市場掃下面停損，取得流動性。

3. **Bullish Engulfing / Displacement**

   * 掃完後出現一根強勢多方 K：

     * 範例條件：當前為多方 K，並且實體吞噬前一根空方 K。

4. **Bullish FVG（Fair Value Gap）**

   * 當前 K 的 low > 前兩根 high 的最大值。
   * FVG 區間：`[max(前兩根 high), 當前 low]`

5. **進場 / SL / TP**

   * 進場價：FVG 區間中點。
   * SL：FVG 下緣（或略低一點 buffer）。
   * TP：RR 1:3（`TP = entry + 3 * (entry - SL)`）。

### 空頭條件（Short Setup）

方向完全相反：

1. NY Session 內。
2. 最近有 sweep high（`high > 前一根 high`）。
3. Bearish Engulfing。
4. Bearish FVG：`high < min(前兩根 low)`。
5. 進場於 FVG 中點，SL 在 FVG 上緣，TP RR 1:3 向下。

### 策略特色與優化方向

* 優點：

  * 適合高波動時段（NY 開盤），有清楚的 edge 假設。
  * SL 與 TP 相對明確，不模糊。
  * 容易可視化於 TradingView，方便人工理解。

* 可能問題：

  * 訊號過少（Session+條件限制較嚴格）。
  * 對某些盤整日，可能出現連續 SL。

* 未來優化：

  * 新增結構過濾（只順日線方向）。
  * 使用 OB 作為 TP 目標，而非固定 RR。
  * 加入 Time Filter（只做當天第一筆 sweep 等）。

---

## 風險控管與安全設計

### 1. 資金與倉位管理

* 單筆風險：不超過帳戶淨值的 1–2%。
* 最大同時開倉數量：可於 config 中設定上限。
* 單日最大虧損：達到某個百分比後關閉當日新單。

### 2. 技術與操作風險

* API Key 安全：

  * 不 commit 到 Git。
  * 盡量啟用 IP 白名單。
  * 可考慮使用子帳戶。

* 程式錯誤：

  * 透過 logging + exception handling 捕捉錯誤。
  * 錯誤發生時 bot 應該：

    * 先嘗試安全停止下單
    * 通知使用者
    * 視情況嘗試重啟 loop

* 交易所風險：

  * API 請求失敗時需有退讓策略（重試 / 暫停）。
  * 嚴禁在 API 狂炸錯誤時無限重試導致被封 IP。

---

## 監控、告警與維運

### 1. 日誌紀錄（Logging）

* 重要事件：

  * 啟動 / 停止 bot
  * 新單建立、平倉
  * 重大錯誤與 exception
* 建議：

  * 將 log 分級（INFO / WARNING / ERROR）
  * 可考慮輪替 log 檔，避免佔滿磁碟

### 2. 即時告警

* 使用 Telegram Bot / Discord Webhook：

  * every trade → 簡要通知
  * error → 高亮通知
  * 若 bot 異常停止，可在外層守護程式偵測並提醒。

### 3. 維運流程

* 定期檢查：

  * VPS 更新與安全性
  * 套件版本（避免因更新導致不相容）
* 版本管理策略：

  * 將「實盤版本」固定在特定 Git tag 或 branch（例如 `prod`）
  * 開發新功能在 `dev` branch，測試無誤才 merge 到 `prod`。

---

## 未來擴充方向

1. **多策略框架**

   * 支援同一套架構下掛多個策略、連多個帳戶。

2. **Dashboard / 可視化**

   * 使用簡易 Web UI 或 Grafana 來顯示：

     * 即時部位
     * 當日盈虧
     * 策略績效

3. **更進階的風控**

   * 動態調整風險比例（例如連續虧損時自動降低倉位）。
   * 用機器學習或統計檢驗策略是否 degrade。

4. **自動報表與回顧**

   * 每日 / 每週自動產出簡報（文字+圖）。
   * 提供策略調整的建議指標。

---

> **總結：**
> 這份企劃的核心目的是：
> 不是只寫一個「能下單的程式」，而是建立一套
> **可回測、可驗證、可持續優化、可安全實盤** 的完整自動交易架構。
>
> 策略可以換、技術可以升級，但這套「開發流程 + 專案架構 + 風控思維」會成為你之後所有量化系統的基礎骨幹。

```

如果你要，我可以下一步直接幫你把這份企劃書拆成：

- `README.md`（對外說明版，偏精簡）
- `docs/architecture.md`（專案架構細節）
- `docs/strategy_ict_ny_fvg.md`（策略專章）

讓你直接丟進 Git repo 當專案文件。
::contentReference[oaicite:0]{index=0}
```
